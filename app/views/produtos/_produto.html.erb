    <tr>
        <td><%= produto.nome %></%></td>
        <td><%= produto.descricao %></%></td>
        <td><%= produto.preco %></%></td>
        <td><%= button_to 'Deletar', produto, method: :delete,
                                    class: "btn btn-danger", data: { confirm: "Deletar #{produto.nome}?" } %>
        </td>
        <td>
            <%= link_to 'Atualizar', edit_produto_path(produto), class: "btn btn-primary"  %>
        </td>
    </tr>